name: CI/CD For WS
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
    
      - name: Docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        
      - name: Build and Push 
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile/Dockerfile.ws.prod
          push: true
          tags: msvaibhav4141/probo-ws:${{ github.sha }}

      - name: SSH to instance
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          script: |
             if [ "$(sudo docker ps -aq -f name=prows)" ]; then
               echo "Stopping and removing existing container..."
               sudo docker rm -f prows
             else
               echo "No existing container found. Proceeding to run a new one."
             fi
             sudo docker run -d \
               --name prows \
               --network probo \
               -e REDIS_URL=${{ secrets.REDIS_URL }} \
               -e DATABASE_URL=${{ secrets.DATABASE_URL }} \
               -p 8080:8080 \
               msvaibhav4141/probo-ws:${{ github.sha }}
         